Activity 11: On Click

Aim: Create a button that increments a counter when clicked, and another button that decrements the counter when clicked

//Create a variable for the increment button, the decrement button, the counter, and the returning count using "querySelector"

//Add and event listener for the increment button using "addEventListener("click", function() {})"

//Increment the counter inside the function using "count++"

//Return the new count by setting the text content of returning count to count using "countReturn.textContent = count;"

//Add and event listener for the decrement button using "addEventListener("click", function() {})"

//Add an if statement using "if (count > 0) {" so the count is only decremented if it is greater than o"

//Inside the if statement, decrement the counter using "count--"

//Return the new count by setting the text content of the returning count to count using "countReturn.textContent = count;"