Activity 7:Look Ma, No HTML

Aim: Use DOM methods to create an entire HTML Page

//Create h1, h2, img, p, and ol elements using document.createElement("type_of_element") and set the element to a variable

//Append each element we just created to the document using document.body.appendChild("element_var_name")

//Add text content to the h1, h2, and p elements using ".textContent = ' ' "

//Center the h1, h2, and p elements using ".style.textAlign = 'center' "

//Add some additional styling to the h1, h2, and p elements using ".style.color"

//Set the src of the image using ".setAttribute("src", "https...");"

//Center the image by setting the display to block, setting the width of the image to 75", and setting the left and right margins to auto using ".setAttribute

//Create 3 li elements and append them as Children under the ol element.

//Give the list a title by adding text content to the ol element using ".textContent"

//Add some additional styling to the list title using ".style.fontSize" and ".style.fontWeight"

//Set the text content for the list items using ".textContent"